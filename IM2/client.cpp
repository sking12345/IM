
#include "component/socket.h"
#include <iostream>
using namespace std;

#define PORT 8000

int main() {

	const char *ip = "127.0.0.1";
	int sfd = tcp_client_init(ip, PORT);
	tcp_client_start(sfd);
	int opt = get_send_buf_size(sfd);

	printf("SO_KEEPALIVE Value: %d\n", opt);

	// sleep(3);
	const char *str = "write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时write()函数从buf写数据到fd中时其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针write()函数从buf写数据到fd中时，其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针（也就是第二个参数buf）不会自动移动其读位置指针（也就是第二个参数buf）不会自动移动若buf中数据无法一次性读完，那么第二次读buf中数据时，其读位置指针（也就是第二个参数buf）不会自动移动，需要程序员编程控制，write()函数从buf写数据到fd中时，若buf中数据无法一次性读完，那么第二次读buf中数据时，其读位置指针（也就是第二个参数buf）不会自动移动，需要程序员编程控制，write()函数从buf写数据到fd中时，若buf中数据无法一次性读完，那么第二次读buf中数据时，其读位置指针（也就是第二个参数buf）不会自动移动，需要程序员编程控制，";


	printf("str len:%ld\n", strlen(str));
	int count = 4;
	int len = strlen(str);
	char *buf = (char*)malloc(len * count + 1);
	memset(buf, 0x00, len * count + 1);
	printf("%d\n", len * count + 1);
	for (int i = 0; i < count; ++i)
	{
		memcpy(buf + i * len, str, len);
	}
	for (int i = 0; i < 2; ++i)
	{
		printf("%d\n", i );
		int status = tcp_client_send(sfd, (char*)buf, len * count + 1);
		if (status < 0)
		{
			printf("%s\n", "error");
		}
	}

	free(buf);
	buf = NULL;
	// for (int i = 0; i < 1000; ++i)
	// {
	// 	// printf("%d\n", i );
	// 	tcp_client_send(sfd, (char*)str, strlen(str));
	// }
	// while (1)
	// {
	// 	char buf[100] = {0x00};
	// 	gets(buf);
	// 	if (strlen(buf) > 0)
	// 	{
	// 		tcp_client_send(sfd, buf, strlen(buf));
	// 	}
	// }
	sleep(2);
	//printf("%s\n", "end0");
	tcp_client_end(sfd);
	printf("%s\n", "end");
	// sleep(2);
	return 0;
}